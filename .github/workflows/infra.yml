name: Terraform Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod
        default: dev
      action:
        description: "Terraform action"
        required: true
        type: choice
        options:
          - plan
          - apply
          - import
        default: plan
      import_address:
        description: "Terraform resource address (for import only)"
        required: false
        type: string
      import_id:
        description: "Azure resource ID (for import only)"
        required: false
        type: string

permissions:
  id-token: write
  contents: read

env:
  TF_VERSION: "~> 1.8"
  WORKING_DIR: infra

jobs:
  terraform:
    name: "Terraform ${{ github.event.inputs.action }} (${{ github.event.inputs.environment }})"
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    defaults:
      run:
        working-directory: ${{ env.WORKING_DIR }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="resource_group_name=${{ secrets.TF_STATE_RESOURCE_GROUP }}" \
            -backend-config="storage_account_name=${{ secrets.TF_STATE_STORAGE_ACCOUNT }}" \
            -backend-config="container_name=tfstate" \
            -backend-config="key=cloudguardiq.${{ github.event.inputs.environment }}.tfstate" \
            -backend-config="use_oidc=true" \
            -backend-config="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}" \
            -backend-config="tenant_id=${{ secrets.AZURE_TENANT_ID }}" \
            -backend-config="use_azuread_auth=true"
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_onboarding_template_uri: ${{ secrets.ONBOARDING_TEMPLATE_URI }}

      - name: Terraform Format Check
        if: github.event.inputs.action != 'import'
        run: terraform fmt -check

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Import
        if: github.event.inputs.action == 'import'
        run: |
          terraform import \
            -var="environment=${{ github.event.inputs.environment }}" \
            "${{ github.event.inputs.import_address }}" \
            "${{ github.event.inputs.import_id }}"
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_onboarding_template_uri: ${{ secrets.ONBOARDING_TEMPLATE_URI }}

      - name: Terraform Plan
        if: github.event.inputs.action != 'import'
        id: plan
        run: |
          terraform plan \
            -var="environment=${{ github.event.inputs.environment }}" \
            -out=tfplan \
            -input=false
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_onboarding_template_uri: ${{ secrets.ONBOARDING_TEMPLATE_URI }}

      - name: Terraform Apply
        if: github.event.inputs.action == 'apply'
        run: terraform apply -input=false tfplan
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_onboarding_template_uri: ${{ secrets.ONBOARDING_TEMPLATE_URI }}